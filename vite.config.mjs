/* Сборка проекта (ADR-0011). Плагинов нет и не будет без отдельного ADR. */
export default {
  // Относительная база: игра должна одинаково открываться из корня, из подпапки
  // GitHub Pages (/cart-rush/) и просто из распакованного архива для модерации
  base: './',

  build: {
    // Бандлы Vite складываем в dist/bundle, а не в dist/assets по умолчанию:
    // в dist/assets ложится наше содержимое public/assets (звуки), и смешивать
    // рукописные ресурсы с генерируемыми — верный способ однажды снести не то
    assetsDir: 'bundle',
    target: 'es2020',

    // Phaser — 1.4 МБ одним куском, и это нормально: движок нужен целиком с первого
    // кадра, дробить его на чанки значит добавить круги загрузки без выигрыша
    chunkSizeWarningLimit: 2000
  }
};
